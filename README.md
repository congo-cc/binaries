This is a repository to hold binary files needed by congo-cc projects. I say "binary files" in the plural
but, for the moment, the only file that will be housed here is `congocc.jar`. Consuming projects can use
it via the git submodule functionality, so that any snapshot of the consuming project's repository will
point to the relevant version of the file..
